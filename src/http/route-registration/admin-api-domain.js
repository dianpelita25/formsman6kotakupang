import { registerAdminApiRoutes } from '../routes/admin-api-routes.js';

export function registerAdminApiDomainRoutes(app, deps) {
  const {
    requireAuth,
    requireSuperadmin,
    jsonError,
    loginWithEmailPassword,
    logout,
    getCookie,
    setCookie,
    deleteCookie,
    SESSION_COOKIE_NAME,
    buildSessionCookieOptions,
    hasSuperadmin,
    listAllSchools,
    createNewSchool,
    patchSchool,
    createSchoolAdminAccount,
    listAllTenants,
    createNewTenant,
    patchTenantById,
    createTenantAdminAccount,
    ensureLegacySchoolFormVersions,
    ensureTenantQuestionnaireInitialized,
    getAiPromptBundle,
    saveAiPromptDraft,
    publishAiPrompt,
    getAiPromptHistory,
  } = deps;

  registerAdminApiRoutes(app, {
    requireAuth,
    requireSuperadmin,
    jsonError,
    loginWithEmailPassword,
    logout,
    getCookie,
    setCookie,
    deleteCookie,
    SESSION_COOKIE_NAME,
    buildSessionCookieOptions,
    hasSuperadmin,
    listAllSchools,
    createNewSchool,
    patchSchool,
    createSchoolAdminAccount,
    listAllTenants,
    createNewTenant,
    patchTenantById,
    createTenantAdminAccount,
    ensureLegacySchoolFormVersions,
    ensureTenantQuestionnaireInitialized,
    getAiPromptBundle,
    saveAiPromptDraft,
    publishAiPrompt,
    getAiPromptHistory,
  });
}
